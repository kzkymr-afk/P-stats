# P-stats 引継書（次チャット・開発者向け）

更新目安: 2026-03 時点の実装・仕様を反映。

---

## 1. プロジェクト概要

- **製品**: パチンコ実戦記録・分析アプリ（SwiftUI / SwiftData / iOS）
- **リポジトリ**: `P-stats`（Xcode プロジェクト `P-stats.xcodeproj`）
- **AI 補助**: ルート `.cursorrules` → 用語は **`用語定義.md`** を参照

---

## 2. 機種マスター（スプレッドシート → JSON → アプリ）

### 2.1 単一の正データ形式: `MachineFullMaster`

- **JSON**: `master_out/machines/{機種ID}.json` + `master_out/index.json`
- **アプリ**: `MachineDetailLoader` は **`MachineFullMaster` のみ**デコード（ネスト旧形式 `MachineDetail` は削除済み）

### 2.2 スプレッドシート仕様（固定・後方互換なし）

詳細は **`docs/機種マスター1シート仕様.md`**。

| 項目 | 内容 |
|------|------|
| **モード列**（モード0〜7） | `mode_N/表示名/densapo[/ui_role]`。**N は 0〜7**。`mode_0`=通常、`mode_1`〜7=特殊。`ui_role`: n/r/l（省略時: mode_0→通常系、他→RUSH系扱い） |
| **当たり列**（当たり1〜12） | **スラッシュ9項目固定**: `あたりID/あたり名/基本出玉/ユニット出玉/最大連結数/滞在モードID/移行先モードID/分岐ラベル/昇格先あたりID` |
| **bonus_id** | JSON 必須。昇格は `promotion_id` が別行の **`bonus_id` と一致**すること |

### 2.3 変換・配信

| 経路 | ファイル / 設定 |
|------|------------------|
| ローカル / CI | `scripts/convert_master_one_sheet.py` |
| GitHub Actions | `.github/workflows/deploy-master-out-pages.yml`（`MASTER_ONE_SHEET_CSV_URL` 等） |
| GAS | `gas_project/gas.gs`（`buildMachineJsonById` / `parseAtariCell9_` / `parseModeCell_`）。**反映手順**: `gas_project/README.md`（`clasp push`。`.clasp.json` は未コミット、テンプレは `.clasp.json.example`）。**master_out 手動デプロイ**: メニュー「マスター配信」または Webhook — `docs/MASTER_OUT_DEPLOY_GAS.md` |

### 2.4 主要 Swift モデル

- `P-stats/Core/MachineMasterModels.swift` … `MachineFullMaster`, `MasterMode`, `MasterBonus`（**`bonusId` は非 Optional**）
- `P-stats/Core/MachineDetailModels.swift` … **`BonusDetail`** のみ（遊技中 `recordHit` 用）
- `P-stats/Core/MachineDetailLoader.swift` … 取得・バンドル `master_out`

---

## 3. 遊技状態・UI ロール（mode_id 1〜7 でも正しく動かす）

- **`GameLog.currentModeID`**: マスタ上の **mode_id（0〜7）**
- **`GameLog.currentModeUiRole`**: 表示・WinType 用 **0=通常系 / 1=RUSH系 / 2=LT**
- **`setStateAfterHit`** は `next_ui_role`（または推定）で `PlayState` を決定
- **`BonusDetail.resolvedNextUiRole`** … チャンス・RUSH フォーカス遷移に利用
- **RUSH / LT フォーカス** … 当たり一覧は **`log.currentModeID`** でフィルタ（固定 mode 1/2 廃止）

続きから復元: `ResumableState` に `currentModeUiRole` あり（`ResumableStateStore`）。

---

## 4. 最近入れた UX / 永続化まわり

- **起動直後のタップ**: ホーム `mainContent` の **初期 `opacity(0)` を廃止**（オフセットのみアニメ）。`AppBootstrapView` でコンテナ準備後 `Task.yield()`。`LaunchView` 終了時も `yield` 後に `onFinish`
- **オートセーブ**: `ResumableStateStore.autosave(from:force:)`
  - **`inactive` / `background`**: `force: true`（ホーム・実戦両方）
  - **実戦中**: 約 **75秒** タイマー + **最短25秒**の非強制保存スロットル（`PlayView`）
  - セッション確定は従来どおり **`save(from:)`**（スロットルなし）

---

## 5. 触ることが多い画面・ファイル

| 機能 | 主なファイル |
|------|----------------|
| ホーム・タブ | `P-stats/App/HomeView.swift`、`App/SettingsTabView.swift`、`App/HistoryAndManagementViews.swift` など |
| 実戦 | `P-stats/Features/Play/PlayView.swift`, `RushFocusView.swift`, `GameLog.swift` |
| 機種・店舗選択 | `MachineShopSelectionView.swift`, `MachineEditView.swift` |
| マスタ取得 URL | `PresetServiceConfig`, `@AppStorage("machineDetailBaseURL")` |

---

## 6. 既知の注意・未検証

- **Xcode ビルド**: CI/サンドボックス外で実機またはシミュレータ確認推奨
- **マスタ**: 当たり行がある機種は **mode_0 定義必須**（変換スクリプトでエラー）
- **GAS と Python** は同一ルールだが、**GAS 側は重複 bonus_id 等の厳密検証が Python より弱い**（運用上は Python 生成を正とする想定でも可）

---

## 7. clasp 再設定（次チャットで対応推奨）

### 「Project settings not found」の原因と対処

**原因（典型）**

- **`gas_project/.clasp.json` がない** … リポジトリでは `.clasp.json` を **`.gitignore`** しているため、clone しただけでは作られず、別 PC やクリーン環境では消えている
- **`scriptId` が誤り** … スプレッドシート ID と取り違え、または別プロジェクトの ID を書いている
- **作業ディレクトリが違う** … `clasp push` を `gas_project` 以外で実行している

**対処の要点**

正しい **スクリプト ID**（下記取得方法）で `.clasp.json` を用意し、`gas_project` で `clasp push` する。手順の詳細は **`gas_project/README.md`**。

### 再設定の 2 通り（`clasp clone` / `.clasp.json.example`）

| 方法 | 手順の要約 | 注意 |
|------|------------|------|
| **案 A: `clasp clone`** | `cd gas_project && clasp clone <SCRIPT_ID>` … `.clasp.json` が自動生成される | **clone 直後、ローカル `gas.gs` が GAS エディタの内容で上書きされる**ことがある。リポジトリ版が新しければ `gas.gs` を戻してから `clasp push` |
| **案 B: `.clasp.json.example`** | `cp .clasp.json.example .clasp.json` のあと **`scriptId` のみ** GAS のスクリプト ID に書き換え | **`clasp clone` を使わず**に紐づけできる。テンプレはリポジトリにコミット済み（`gas_project/.clasp.json.example`） |

どちらでも最後に `clasp push` で反映確認。

### 現状（GAS / clasp）

- **GAS**: Script Properties（`GITHUB_PAT`, `GITHUB_REPO`）設定済み。GitHub 側の Variables に `MASTER_ONE_SHEET_CSV_URL` 等の設定済み
- **clasp**: 環境ごとに `.clasp.json` が必要。上記エラー時は本節の再設定。**clasp が使えない間**は `gas.gs` を GAS エディタへ**手動コピペ**でも同じ反映が可能

---

## 8. 今回セッションで実施したこと（2026-03 頃）

- **master_out デプロイ**: GAS メニュー「マスター配信」および（設定時）Webhook から、GitHub Actions（`deploy-master-out-pages.yml`）を起動する流れを整備・運用（手順は `docs/MASTER_OUT_DEPLOY_GAS.md`）
- **差分更新**: `convert_master_one_sheet.py` を、変更のあった機種のみ JSON 更新・CSV から消えた機種は JSON 削除、に対応
- **GAS 設定**: Script Properties（PAT / リポジトリ）、GitHub リポジトリ Variables の設定
- **clasp の現状**: リポジトリに `.clasp.json` は含めない方針のまま。**再設定は §7**（`clasp clone` または `.clasp.json.example` コピー）。次チャットでは **§7・§8** を読めば clasp 再設定と本セッションの変更範囲を把握できる

---

## 9. 関連ドキュメント

- `docs/機種マスター1シート仕様.md`
- **`docs/MASTER_OUT_DEPLOY_GAS.md`** … master_out の GAS 連携（メニュー／Webhook、`workflow_dispatch`、Secrets / Variables）
- `docs/GitHubPages_master_out配信手順.md`（あれば）
- ルート `用語定義.md`

---

**次担当者へ**: 仕様変更時は **`convert_master_one_sheet.py` / `gas.gs` / `機種マスター1シート仕様.md` / Swift デコード** をセットで更新してください。**clasp の再設定と今回までの変更の把握**は **§7・§8**、master_out 配信の詳細は **§9** の `docs/MASTER_OUT_DEPLOY_GAS.md` を参照。
