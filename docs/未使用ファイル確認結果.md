# 古いスクリプト・未使用 Swift ファイル 確認結果

実施日: 2025-03 想定

---

## 1. スクリプト（Python）

| ファイル | 参照元 | 判定 |
|----------|--------|------|
| **scripts/update_machines.py** | `.github/workflows/update-master-data.yml`（週次マスタ更新） | ✅ 使用中 |
| **scripts/convert_mode_bonus.py** | `docs/AGENT_INSTRUCTIONS_MachineDetail.md`（MachineDetail → index.json / machines/*.json） | ✅ 使用中 |
| **scraper/Pachi_Collector.py** | `docs/AGENT_INSTRUCTIONS_機種情報ハイブリッド.md`（機種ページ HTML 取得・GAS 連携） | ✅ 使用中 |
| **make_icon_transparent.py**（リポジトリルート） | なし（コードベース・CI・ドキュメントいずれからも参照なし） | ⚠️ **参照なし** |

### make_icon_transparent.py について

- **内容**: 指定画像の白部分を透明化し、1024x1024 のアプリアイコンとして保存するワンオフスクリプト。
- **問題**: 入力・出力パスがハードコード（`/Users/kazukiyoomura/...`）されており、他環境ではそのまま実行できない。
- **選択肢**  
  - **A) 削除する** … もう使わない場合。  
  - **B) 残す** … アイコン差し替え時に手動でパスを書き換えて使う想定なら残し、README や docs に「アイコン生成用・必要時のみ手動実行」とメモする。  
  - **C) 改修する** … 引数で入力画像・出力先を渡すようにし、CI やローカル手順に組み込む。

---

## 2. Swift ファイル（P-stats ターゲット内）

- **確認方法**: 各 .swift で定義されている型（View / enum / class 等）を grep で検索し、他ファイルから参照があるか確認。
- **結果**: **未使用の Swift ファイルはありませんでした。** 34 ファイルすべてが、`P_statsApp` または他の View/Service から参照されています。

※ `Features/Common/InfoPopoverView.swift` はファイル名に "InfoPopoverView" とあるが、中身は `InfoIconView` と `LabelWithInfo` で、`InfoIconView` は PlayView / MachineEditView / AnalyticsDashboardView / MachineShopSelectionView / InsightPanelView などで使用されています。

---

## 3. まとめ

- **削除候補**: **make_icon_transparent.py** のみ（参照なし・ハードコードパス・ワンオフ用途）。
- **Swift**: 未使用ファイルなし。削除の必要はありません。
- **その他**: `.sh` ファイルはプロジェクト内に存在しませんでした。

削除するかどうかは依頼者判断でお願いします。
